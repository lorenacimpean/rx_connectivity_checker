name: basic_connectivity_checker_example
description: Demonstrates how to use the basic_connectivity_checker package.

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  # This dependency points to the root of your local package
  basic_connectivity_checker:
    path: ../
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0